const calc = {
  sum: (a, b) => a + b,
  sub: (a, b) => a - b,
  mult:(a, b) => a * b,
  div: (a, b) => a / b
}

console.log(calc.sum(2, 2))
console.log(calc.sub(2, 2))
console.log(calc.mult(2, 2))
console.log(calc.div(2, 2))
